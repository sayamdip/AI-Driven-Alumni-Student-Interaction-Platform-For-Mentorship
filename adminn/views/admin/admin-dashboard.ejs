<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/css/admin-styles.css"> <!-- External CSS -->
    <style>
        /* Global Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Poppins', sans-serif;
    text-decoration: none;
}

/* Body Background */
body {
    background: #2a3f54;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

/* Dashboard Container */
.dashboard-container {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
}

/* Card Design */
.dashboard-card {
    background: white;
    padding: 30px;
    border-radius: 12px;
    box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
    width: 400px;
    text-align: center;
    transition: transform 0.4s ease, box-shadow 0.4s ease;
}

/* Card Hover Effect */
.dashboard-card:hover {
    transform: scale(1.05);
    box-shadow: 0px 15px 30px rgba(0, 0, 0, 0.3);
}

/* Heading Animation */
h2 {
    color: #333;
    margin-bottom: 20px;
    animation: fadeInUp 1s ease-in-out;
}

/* Navigation Links */
.nav-links {
    list-style: none;
    padding: 0;
    margin-bottom: 20px;
}

.nav-links li {
    margin: 10px 0;
}

/* Links Hover Effect */
.hover-link {
    display: block;
    background: #007BFF;
    color: white;
    padding: 12px;
    border-radius: 5px;
    transition: 0.3s ease-in-out;
}

/* Link Hover Effect */
.hover-link:hover {
    background: #0056b3;
    transform: translateY(-3px);
    box-shadow: 0px 5px 15px rgba(0, 123, 255, 0.8);
}

/* Logout Button */
.logout-btn {
    display: inline-block;
    margin-top: 10px;
    background: #e74c3c;
    color: white;
    padding: 12px;
    border-radius: 5px;
    transition: 0.3s ease-in-out;
}

/* Logout Button Hover */
.logout-btn:hover {
    background: #c0392b;
    transform: scale(1.05);
}

/* Animations */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

    </style>
</head>
<body>

    <div class="dashboard-container">
        <div class="dashboard-card">
            <h2 class="fade-in">Welcome to the Admin Dashboard</h2>
            <ul class="nav-links">
                <li><a href="/admin/alumni/add" class="hover-link">➕ Add Alumni</a></li>
                <li><a href="/admin/alumni/view" class="hover-link">👁️ View Alumni</a></li>
                <li><a href="/admin/alumni/manage" class="hover-link">⚙️ Manage Alumni</a></li>
            </ul>
            <a href="/admin/logout" class="logout-btn">🚪 Logout</a>
        </div>
    </div>

</body>
</html>
