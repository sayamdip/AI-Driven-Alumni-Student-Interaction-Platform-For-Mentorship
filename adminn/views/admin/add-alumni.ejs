<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Therapy</title>

    <style>
        /* Global Styles */
/* Global Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Poppins', sans-serif;
    text-decoration: none;
}

/* Body Background */
body {
    background: #2a3f54;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

/* Dashboard Container */
.dashboard-container {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 100%;
}

/* Card Design */
.dashboard-card {
    background: white;
    padding: 30px;
    border-radius: 12px;
    box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
    width: 400px;
    text-align: center;
    transition: transform 0.4s ease, box-shadow 0.4s ease;
}

/* Card Hover Effect */
.dashboard-card:hover {
    transform: scale(1.05);
    box-shadow: 0px 15px 30px rgba(0, 0, 0, 0.3);
}

/* Heading Animation */
h2 {
    color: #333;
    margin-bottom: 20px;
    animation: fadeInUp 1s ease-in-out;
}

/* Success/Error Message */
.message {
    color: #28a745;
    font-weight: bold;
    margin-bottom: 15px;
}

/* Form Styling */
.alumni-form {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

/* Labels */
label {
    font-weight: bold;
    text-align: left;
}

/* Input Fields */
input {
    padding: 12px;
    border-radius: 5px;
    border: 1px solid #ddd;
    width: 100%;
    font-size: 16px;
    transition: 0.3s;
}

/* Input Hover & Focus */
input:hover, input:focus {
    border-color: #007BFF;
    box-shadow: 0px 5px 10px rgba(0, 123, 255, 0.3);
    outline: none;
}

/* Submit Button */
.submit-btn {
    background: #007BFF;
    color: white;
    padding: 12px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: 0.3s ease-in-out;
}

/* Submit Button Hover */
.submit-btn:hover {
    background: #0056b3;
    transform: translateY(-3px);
    box-shadow: 0px 5px 15px rgba(0, 123, 255, 0.8);
}

/* Back Button */
.back-btn {
    display: inline-block;
    margin-top: 10px;
    background: #e74c3c;
    color: white;
    padding: 10px;
    border-radius: 5px;
    transition: 0.3s ease-in-out;
}

/* Back Button Hover */
.back-btn:hover {
    background: #c0392b;
    transform: scale(1.05);
}

/* Animations */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

        
    </style>
</head>
<body>

    <div class="dashboard-container">
        <div class="dashboard-card">
            <h2 class="fade-in">🩺 Add Alumni</h2>
            
            
            <form action="/admin/alumni/add" method="POST" class="alumni-form">
                <label>Therapist Name:</label>
                <input type="text" name="alumni_name" placeholder="Enter alumni Name" required>
                
                <label>Email:</label>
                <input type="email" name="email" placeholder="Enter Email" required>
                
                <label>Password:</label>
                <input type="password" name="password" placeholder="Enter Password" required>
                <% if (message) { %>
                    <p class="message"><%= message %></p>
                <% } %>

                <button type="submit" class="submit-btn">➕ Add Alumni</button>
            </form>

            <a href="/admin/dashboard" class="back-btn">⬅️ Back to Dashboard</a>
        </div>
    </div>

</body>
</html>
